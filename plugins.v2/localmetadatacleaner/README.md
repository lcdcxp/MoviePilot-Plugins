## v2.8.3 最近记录显示修复
- 修复详情页下方“最近记录”始终显示为空的问题；最近处理记录和较早失败记录现在会正确读取、去重并分组展示。
- 无需清空历史；升级后，原来已经保存的记录会直接恢复显示。
- 本次不改变刮削、队列、路径映射及 `.nfo` 处理流程。

## v2.8.1 重复入库去重与复查合并
- 通知层只保留最终成功、最终复查结果和真正失败；“加入 10 天复查”“10 天复查后创建补刮削任务”“目录检查后创建单集补刮削任务”等创建/排队类中间态不再发送站内通知。
- 刮削成功通知改为图标化清爽模板，自动兼容单集、批量多集、电影以及整剧/季信息/单集图片混合完成场景。
- 详情页标题改为“最近记录”，同一集的排队补刮、触发刮削、检查完成/失败合并到同一个面板，避免同一集重复显示。
- 修复电影补刮/电影重试成功已有通知模板但未进入通知入口的问题；电影成功会使用新的电影刮削成功模板。
- 单集实际触发刮削记录补充集数 payload；最近记录 10 分钟去重改为合并集数而不是覆盖，避免同剧不同集互相顶掉。
- 目录级入库事件按整剧/Season 目录做近期去重，重复目录事件不再绕过去重保护。
- 新增电视剧入库事件短时间冷却：同一剧同一批集数在冷却窗口内再次入库时，不再重复触发整剧/单集刮削。
- 新增单集刮削成功记录：近期已成功触发过的同一集再次缺图时，合并等待原 10 分钟检查，不重复发送刮削事件。
- 10 天复查按剧名目录跨 batch 合并；重复缺图集只合并到已有复查任务，不再重复发送“已列入10天复查”通知。

## v2.7.12 统一为四种语义色
- 长期复查顶部指标卡改为 CloudDrive2 最近操作同款 `warning` 黄色。
- 详情页颜色统一收敛到 `success` 绿、`info` 蓝、`warning` 黄、`primary` 紫；删除、失败、等待重试、普通记录等状态不再使用额外红色或灰色。

## v2.7.11 统一紫色为 primary
- 历史记录标题图标与编号、`已合并通知` chip、单集刮削 chip、整组执行按钮和刮削集标签统一改用 MoviePilot/Vuetify 的 `primary`。
- `purple` / `deep-purple` 只保留为兼容别名，统一映射到 `primary`，不再使用旧的 `#9C27B0` 紫色。

## v2.7.10 顶部指标卡按 CloudDrive2 源码配色
- 顶部四个指标卡按 CloudDrive2通知的 `_stat_card()` 写法调整：`VCard` 直接使用 `variant="tonal"` 和 `success` / `info` / `success` / `primary` 语义色。
- 移除顶部指标卡额外背景色、图标色和值颜色覆盖，让 MoviePilot/Vuetify 用和 CloudDrive2 相同的 tonal 渲染。

## v2.7.9 修正触发刮削通知语义
- 触发刮削结果只在详情页作为处理记录展示，不再标成“已合并通知”。
- 通知入口排除“已触发刮削”结果，避免非最终节点发送站内通知。

## v2.7.8 参考配色重做详情页
- 按 CloudDrive2通知的详情页代码，把顶部指标改成整块 `tonal` 彩色卡，图标直接用 `VIcon` 的 `text` 渲染，不再使用圆形头像包裹。
- 按聚影签到的详情页代码，把待处理任务和历史记录区域改成 `flat/surface` 白色卡，标题使用彩色 `VIcon`，清空按钮、chip 和空状态使用更明显的浅色块。

## v2.7.7 全面检查与代码清理
- 全面复查详情页代码，删除旧版未调用的运行概览、路径规则和媒体库映射检测函数，避免源码里残留旧卡片文案干扰排查。
- 保留 v2.7.6 的 MoviePilot 兼容图标写法：`VIcon` 使用 `text` 承载 `mdi-*` 图标名。

## v2.7.6 详情页图标兼容修复
- 按 MoviePilot 插件常见写法修复详情页顶部指标和空状态图标：`VIcon` 使用 `text` 承载 `mdi-*` 图标名，避免 `props.icon`、按钮图标或 MDI 字体类在详情页渲染为空圈。
- 保留 v2.7.5 的可展开任务/历史详情、隐藏媒体库映射检测卡和截图式布局。

## v2.7.5 真实详情展开修复
- 恢复 MoviePilot 详情页里的任务/历史展开面板，点击后可以查看表格明细、任务路径、检查范围和历史子记录。
- 隐藏详情页顶部的媒体库映射检测卡，避免偏离参考截图；顶部指标图标改用按钮图标方式渲染，修复空圆问题。

## v2.7.4 按截图重排详情页
- 详情页按参考截图重排为监控工作台：顶部四个指标卡、待处理任务表格、折叠任务行、最近记录步骤清单和绿色集数标签。
- 移除上一版偏“渐变卡片”的观感，统一为白底、细边框、小面积低饱和状态色。

## v2.7.3 彩色工作台详情页
- 按参考图恢复详情页颜色层次：顶部指标卡、待处理任务、历史记录、状态标签和操作按钮使用低饱和蓝/绿/橙/紫/红配色。
- 保持白底细边框、轻阴影和小面积状态色，不铺大面积高亮色块，整体更接近监控面板。

## v2.7.2 详情页视觉柔化与素色版
- 插件详情页移除大面积绿色/蓝色提示块，空队列和空历史改为白底描边卡片，只保留小图标圆点提示状态。
- 彩色版使用低饱和的靛色、蓝灰、琥珀、青绿色作为小面积强调，不再用鲜绿/亮蓝铺满提示区域。
- 素色版把详情页 chip、按钮、指标图标和空状态统一映射为灰色，适合不想看到彩色状态提示的用户。
- 两个安装包功能完全一致，只区分详情页观感；通知合并、折叠明细、立即执行/检查/复查/重试/删除/清空等按钮均保留。

## v2.6.3 队列一致性与路径边界修复
- 空电视剧目录、空长期复查目录和缺少 Season 范围的任务改为有限重试，耗尽后明确失败，不再无限等待或误报成功。
- 电影、电视剧、单集延迟任务及长期复查执行前重新确认对应 STRM 仍存在且位于原影片/剧季/配置根路径内；已删除、改名或串到其他剧季的旧任务不会操作 CD2。
- 同一电影的初始任务、文件重试任务及升级前重复任务会合并为一条，避免向 MoviePilot 重复发送刮削事件。
- 单集 CD2 目标除根路径和视频后缀外，还必须与当前 STRM 映射到同一目录且严格同名，避免损坏的旧任务误删或误刮其他集。
- CD2 刮削、目录文件枚举与 NFO 删除增加符号链接/目录联接解析后的真实路径边界，阻止通过链接跳出配置根目录。
- 修复 STRM 根路径配置为 `/`、媒体库缓存时间位于未来、路径刷新缓存跨插件实例共享和历史去重记录顺序不正确的问题。
- 通知不再把“MoviePilot 事件发送成功”直接称为最终刮削成功：电影显示“已触发刮削”，电视剧在刮削后检查确认元数据生成后显示“刮削成功”。
- 保留旧配置、旧历史和现有待处理任务兼容；升级到本版本不要求清空数据。

## v2.6.2 季信息重试与通知准确性修复
- 移除“触发 MP 刮削”开关，插件启用后固定使用 MP 刮削功能；旧配置中的 `scrape` 字段继续兼容，但不再允许关闭。
- 季信息刮削后仍不完整时只额外补刮一次；再次复查仍缺失会结束任务并发送失败通知，避免无限循环创建 Season 补刮任务。
- 修复不同媒体库同名电视剧可能合并成一条通知、电影队列异常可能误报为电视剧的问题。
- 长期复查任务保存创建时的复查天数，后续修改配置不会改变原任务通知中的天数；旧任务会尽量从创建和到期时间还原。
- 修复异常未来时间戳可能长期命中刮削去重、路径刷新缓存或历史去重的问题。
- **升级后建议在插件详情页清空历史记录；如果仍存在旧版本待处理任务，同时清空待处理队列，避免旧任务继续按旧状态运行。**

## v2.6.1 队列、复查与通知闭环修复
- 插件详情页改为只读状态快照，打开页面不会覆盖并发队列更新，也不会提前删除尚未到期的长期复查任务。
- 自动队列按任务隔离异常；单个损坏的旧任务不会阻塞后续任务，连续异常会有限重试并给出最终通知。
- 10 天复查任务保留到真正到期，由到期检查统一通知“已恢复”或“仍缺图”；STRM 剧名目录暂不可访问时先有限重试。
- 复查任务创建会返回真实入队结果，避免图片恰好生成时仍误报“已列入复查”。
- 复查天数拒绝负数、NaN 和无穷值，目录刮削去重标记会定期清理，避免状态长期增长。
- 删除单集 NFO 时兼容严格同名的 `.NFO` 等大小写形式；仍只允许删除 CD2 刮削根路径内、本次真实媒体对应的同名 NFO。
- 保留旧队列、旧历史动作标签和旧配置字段兼容，无需清理插件历史数据或待处理任务。

## v2.6 简洁结果通知与旧数据兼容
- 新增简洁通知：刮削成功、列入长期复查、长期复查完成/仍缺图，以及最终刮削失败。
- 同一次队列处理中，同一部电视剧的多个单集会合并成一条通知，最多展示前 8 集并标注总数。
- 短期重试、完整跳过和普通检查记录不发送通知，完整路径与技术细节继续保留在插件详情页和日志中。
- 详情页统一显示“刮削成功、等待长期复查、复查完成、待补刮削、需关注”等结果状态。
- 不迁移、不清空现有历史和待处理任务；新通知会从旧任务已有的 `scope`、`show_root`、`episode_strm` 等字段回退识别媒体名称与集号。

## v2.5 MoviePilot list_files 兼容修复
- 适配 MoviePilot v2.13.10 的 `StorageChain.list_files(fileitem)` 调用方式。
- CD2 路径预热/探测时先通过 `get_file_item(storage, path)` 获取 `FileItem`，再调用 `list_files`，避免将 `str` / `PosixPath` 直接传入导致 `object has no attribute 'storage'` 通知。

## v2.4.1 路径安全边界修复
- 路径判断新增 `.` / `..` 段拦截，避免畸形路径绕过 CD2-only 刮削和 nfo 删除安全校验。
- 修复 STRM 检查根路径配置为 `/` 时，映射到 CD2 根路径少一个斜杠的问题。

## v2.4 CD2 单集 nfo 删除安全修复
- 单集缺图、目录刮削后复查缺图、10 天复查缺图时，只在真实 CD2 单集视频已找到且通过安全校验后，才会在触发单集刮削前删除 CD2 同名 `.nfo`。
- 单集 nfo 删除目标优先绑定本次真实刮削媒体文件的同名 `.nfo`，避免旧队列或配置变化时删除路径和实际刮削路径不一致。
- 删除目标必须位于 CD2 刮削根路径下、不能位于 STRM 媒体库根路径下，且后缀必须是 `.nfo`；删除失败或 CD2 父目录未确认刷新成功时，不触发刮削，进入短期重试或记录最终失败。
- 如果 CD2 单集 nfo 路径存在但暂时不能确认为文件，也会进入短期重试，不会把该状态当作 nfo 不存在继续刮削。
- 剧/季目录刮削新增 CD2-only 安全校验：目标必须位于 CD2 刮削根路径下，不能位于 STRM 媒体库根路径下，避免配置异常或旧任务误刮 STRM 目录。
- 如果前一次已删除 nfo、后续 MP fileitem 暂不可用进入重试，历史记录会继续保留“此前已删除 CD2 同名 nfo”的提示。
- 手动删除 Season 相关队列时，会同步清理 `scope_dir` / `season_dirs` 对应的 10 分钟去重 marker，避免重新入库同季被旧 marker 合并跳过。
- 单集任务创建会复用初始检查阶段已确认的 CD2 真实媒体目标；目标为空或未通过安全校验时仍回退到原有查找逻辑。

## v2.2.3 单集旧队列安全校验

- 电视剧单集旧队列中的 `scrape_target` 现在必须通过安全校验：只能位于 CD2 刮削根路径下，不能位于 STRM 媒体库根路径下，且必须是真实视频后缀。
- 如果旧任务残留了非 CD2 的 mkv/mp4 等路径，插件会拒绝触发刮削并移除异常任务，避免误刮非目标路径。

## v2.2.2 CD2 文件级单集刮削修复

- 单集真实文件通过 StorageChain 就绪时，刮削事件固定传入真实媒体文件路径，避免 FUSE `Path.is_file()` 短暂不可见导致 `file_list` 为空。
- 单集候选查找不再被父目录本地可见性卡住；即使 Season 目录暂不可见，也会逐个尝试同名 mkv/mp4/ts/m2ts/iso 等候选文件。

## v2.2.1 CD2 文件级预热缓存修复

- 真实媒体文件预热不再降级成父目录成功：父目录非空只能作为预热信息，不能代表目标电影/单集文件已经就绪。
- 只有目标媒体文件本身存在，或 MP StorageChain 能获取目标文件 fileitem，才缓存为成功。
- 同批次刷新失败不再标记为“本批次已刷新过”，避免后续少一次探测机会。

## v2.2 CD2 按层级预热与电视剧短期重试

- 修正 zip 打包路径，插件目录使用 Linux 可识别的 `/` 路径。
- CD2 路径预热改为按具体路径去重：剧目录、Season 目录、单集真实视频目录分别允许刷新，不再一次刷新覆盖整批任务。
- 刷新成功才长时间缓存；刷新失败只短暂缓存，避免 CD2 暂未就绪时 600 秒内不再尝试。
- 电视剧剧名目录、Season 目录、单集真实视频暂不可访问时，进入 30秒/2分钟/5分钟/15分钟/60分钟短期重试。
- 目录刮削前如果目录下媒体文件列表为空，视为 CD2 尚未完全就绪，不发送空刮削事件。
- 10 天复查任务手动按钮改为“立即复查”，会执行完整复查流程：检查缺图、重新创建单集刮削任务，并在实际单集刮削前删除 CD2 同名 nfo。
- 剧信息判断排除 `seasonXX-poster` 等季级图片，避免季海报误判为剧信息完整。


## v2.1 电影同名真实文件重试修复

- 只修改电影补刮削逻辑，电视剧 v2.0 的剧/季/集处理流程未改动。
- 电影缺刮削信息时，不再扫描视频目录选最大文件，也不直接刮削电影目录。
- 插件会用 STRM 文件名 stem 映射到 CD2 目录，逐个尝试 `.mkv/.mp4/.ts/.m2ts/.iso/.mov/.avi/.rmvb` 等同名真实视频后缀。
- 每个候选路径直接交给 MP StorageChain 获取 fileitem，不再只依赖 `Path.exists()` 判断 CD2 文件是否可见。
- 如果 CD2 文件暂未刷新出来，会进入短期重试：30 秒、2 分钟、5 分钟、15 分钟、60 分钟；多次仍失败才记录最终失败。
- 安全保护：电影刮削目标必须是 `/CD2` 刮削目标根路径下的真实视频文件；禁止 `.strm`、禁止 `/media`、禁止 STRM 目录作为刮削目标。

## v2.0 电视剧范围判断与补刮削流程重构

- 电视剧入库后先判断范围：整剧目录入库扫描整剧，整季/批量多集扫描当前季，追更单集只检查单集。
- 按剧信息、季信息、单集图片三层独立检查，完整的部分不重复刮削，缺失的部分才补刮削。
- 剧信息缺失刮削 CD2 剧名目录，季信息缺失刮削 CD2 Season 目录，单集缺图刮削真实单集媒体文件。
- 如果本次已触发剧/季目录刮削，被目录覆盖的缺图集先等待 10 分钟检查，仍缺图才创建单集补刮削任务；实际刮削前删除 CD2 对应单集 nfo，避免一次入库重复刮单集。
- 10 天复查只针对单集：10 分钟后仍缺图的集进入复查，复查时重新创建单集刮削任务，并在实际刮削前删除 CD2 对应单集 nfo。
- 电影逻辑未改动。

# 监控strm刮削网盘

## v1.9 电视剧三层检查逻辑重构

- 电视剧入库后按本次 STRM 快照处理，不再重新扫描整部剧，避免第一季任务扫到后加入的第二季。
- 按“剧信息 → 季信息 → 单集图片”三层检查：剧/季信息缺失时按 MP 原生模式使用 CD2 剧名目录/Season 目录触发 MP 刮削；不使用真实单集文件兜底；不会中断后续单集检查。
- 缺图单集会在真实单集文件刮削前删除 CD2 对应同名 `.nfo`。
- 剧/季目录刮削不会跳过任何单集；所有缺图集仍会进入 10 分钟图片检查；10 分钟后仍缺图才加入 10 天复查。
- 保留媒体库路径映射、入库日志精简、电影真实文件刮削、待处理任务分组和 UI 优化。

## v1.6 调试、媒体库匹配与详情页优化

- 新增入库事件 INFO 调试日志，显示 event、channel、path、library。
- 放宽 channel 限制，不再只允许 emby，由 STRM 路径、媒体库路径映射和触发媒体库配置决定是否登记任务。
- 忽略入库事件时输出原因：未取得媒体路径、媒体库不匹配、路径不在 STRM 根路径、路径层级不足等。
- 新增媒体库路径映射检测，保存配置后可检测每行格式、movie/tv 类型、路径是否在 STRM 根路径下。
- 详情页待处理任务按类型分组，并显示每类任务的下一步处理逻辑。
- 历史记录新增最近失败/异常记录区域，便于优先排查刮削失败、目标缺失和复查缺图。

本插件适用于想要整理刮削网盘的用户。它复用 MoviePilot 全局媒体库入库事件，依赖“媒体库服务器通知”插件和 CloudDrive2（CD2）：收到 Emby 入库通知后，先检查 STRM 路径中的刮削信息是否完整；判断需要刮削时，再映射到 CD2 挂载的网盘真实路径并触发 MP 刮削。

## 前提

- MP 已启用媒体库服务器通知插件，且能收到 Emby 新入库通知。
- MP 必须映射 STRM 文件夹和 CD2 文件夹。
- 建议 MP 的 STRM 映射路径与 Emby 保持一致，例如 Emby 使用 `/media`，MP 也映射为 `/media`。

## 推荐配置

- STRM 检查根路径：`/media`
- MP 刮削目标根路径：`/CD2/115/CMS影库/影视`
- 入库后检查延迟秒：`10`
- 单集刮削等待秒：`30`
- 复查天数：`10`（用于电视剧/番剧缺图集复查）
- 兜底检查周期：建议 `*/30 * * * *` 或 `*/10 * * * *`，只用于补跑 MP 重启、保存配置或定时器丢失后未执行的到期任务；正常的 10 秒、30 秒、10 分钟任务会按到期时间自动执行。
- 媒体库路径映射：可选，用于 Emby 媒体库名称和 STRM 实际路径不一致的情况。格式为 `媒体库名称|类型|路径1,路径2`，类型为 `movie` 或 `tv`。例如：`动漫|tv|/media/电视剧/国漫,/media/电视剧/日番`。修改映射后先保存配置，再点击“检测已保存映射”。

路径示例：

`/media/电影/华语电影/片名/xxx.strm`

检查目录：`/media/电影/华语电影/片名`

刮削目标：优先使用 `/CD2/115/CMS影库/影视/电影/华语电影/片名/xxx.mkv` 等真实媒体文件

## 逻辑

电影：检查片名目录是否同时存在 backdrop、fanart、poster/folder/cover 类图片和任意 `.nfo`。完整跳过；不完整时只用 STRM 同名规则生成 CD2 真实视频候选，逐个请求 MP fileitem 成功后触发刮削。若 CD2 暂未刷新出真实文件，会进入短期重试；不会刮削 STRM 文件、STRM 目录或 `/media` 路径。

电视剧/番剧：按“剧名目录 + 季目录”合并入库事件，并保存本次入库 STRM 快照。后续检查只处理这批集数，不会重新扫描整部剧。

- 剧信息检查：检查剧名根目录是否已有图片/nfo。缺失时，按 MP 原生模式刮削 CD2 剧名目录；目录刮削失败时只记录失败，不使用真实单集文件兜底；不会因为触发了剧信息刮削就停止后面的季信息和单集检查。
- 季信息检查：检查本次涉及的 `Season N`，要求剧名根目录存在对应 `season02-poster.*` 这类季海报，并且 `Season N` 目录存在 `poster.*` 和 `season.nfo`。缺失时，按 MP 原生模式刮削 CD2 Season 目录；目录刮削失败时只记录失败，不使用真实单集文件兜底。
- 单集图片检查：遍历本次入库快照里的所有单集，STRM 同名图片存在则判定完成；缺图则创建单集刮削任务，真实 CD2 单集视频已找到且通过安全校验后，才会在刮削前删除 CD2 对应同名单集 `.nfo`。同一批次同一个真实媒体文件只发送一次刮削事件，但该集仍会进入 10 分钟检查。
- 10 分钟检查：单集刮削事件发送成功后才开始计时；10 分钟后仍缺图才加入 10 天复查。手动提前检查仍缺图时只保留原任务，不会提前加入 10 天复查。
- 10 天复查：只处理 10 分钟检查后仍缺图的单集，重新创建单集刮削任务，并在真实单集刮削前删除 CD2 对应 `.nfo`，不会整剧重刮。插件详情页会按当前 STRM 状态刷新 10 天复查任务，已经生成图片的集会自动移出；全部完成则删除该复查任务。

单集刮削会把 STRM 路径映射到 MP 刮削目标根路径后，再把 `.strm` 替换成同名 `.mkv/.mp4/.ts/.m2ts/.iso` 等视频格式逐个查找；不会读取 STRM 内容里的 HTTP 短链文件名。找不到同名真实媒体文件时会通知失败并跳过，不再退回刮削 Season 目录，避免误刮整季。

媒体库过滤会同时识别路径第一层和第二层，例如 `/media/电视剧/国产剧/...` 可以命中 `电视剧` 或 `国产剧`。如果媒体库名称和路径不一致，请使用“媒体库路径映射”，例如 Emby 媒体库叫 `动漫`，但实际路径是 `/media/电视剧/国漫` 和 `/media/电视剧/日番`，填写 `动漫|tv|/media/电视剧/国漫,/media/电视剧/日番` 后，选择触发媒体库 `动漫` 也可以正常匹配。

## 入库事件排查日志

插件会在 INFO 日志中记录每个入库事件的关键判断过程：

```text
收到入库事件：event=xxx，channel=xxx，path=xxx，library=xxx
入库事件判断结果：已登记，任务：xxx
```

如果未登记，会显示原因，例如：

```text
入库事件判断结果：已忽略，原因：媒体库不匹配
入库事件判断结果：已忽略，原因：路径不在STRM根目录
入库事件判断结果：已忽略，原因：路径层级不足
入库事件判断结果：已忽略，原因：未取得媒体路径
```

入库事件不再只限制 `channel=emby`，不同媒体服务器或通知插件传入的 channel 会先放行，再由 STRM 路径、媒体库过滤和媒体库路径映射决定是否登记任务。

## v1.3 修复重点

- 修复同一部剧新旧批次合并后，新集被旧 10 分钟任务提前检查的问题。
- 修复图片已生成后，旧 10 天复查任务不自动清理的问题。
- 修复第二季等具体季号被通用 `season-poster.*` 误判为季海报完整的问题。
- 增加季信息刮削后的 10 分钟检查。


## v1.3 补充功能
- 历史记录新增具体集数展示，刮削后检查、10天复查、缺图入队等记录会显示检查集/缺图集。
- 电视剧任务按剧名和季目录隔离，避免第一季检查时扫描后加入的第二季。
- 待处理任务支持单条立即执行/立即检查，检查类任务提前执行时不会破坏原有10分钟和10天复查逻辑。
- 新增媒体库路径映射配置，支持一个媒体库对应多个 STRM 路径，并可指定 `movie` / `tv` 处理逻辑。

## v2.7.1 工作台视觉修正
- 顶部改为四个指标卡：待处理、10分钟检查、长期复查、最近成功。
- 待处理任务和最近记录改为编号工作台区块，空队列和空历史也使用新版空状态，避免看起来仍是旧页面。
- 媒体库映射检测改为摘要折叠面板，默认不再铺开多行明细。

## v2.7 通知合并与详情页折叠明细
- 正式整理为 `2.7` 版本：保留 v2.6.8 的通知合并、待处理任务折叠明细和历史记录聚合展示能力。
- 安装包、插件声明版本、`package.json` 顶层版本和 README 版本记录均已同步到 `2.7`。

## v2.6.8 通知合并与详情页折叠明细
- 同一部电视剧在同一批处理中产生整剧、季信息、单集检查成功时，会合并成一条通知，正文展示范围、集数和合并记录数，避免一部剧连续通知多次。
- 插件详情页的待处理任务改为折叠面板：默认按类型/剧名展示摘要，展开后显示每一集、到期时间、刮削目标、当前状态、失败原因。
- 原有操作按钮继续保留：立即执行、立即检查、立即复查、立即重试、删除任务、删除整组、清空队列。
- 最近记录/历史记录改为按同一媒体聚合展示，失败/异常仍优先列出；展开后可查看整剧刮削、季信息检查、单集检查、长期复查和失败记录等子记录。

## v2.6.7 logo 刮削触发路径修复
- 插件触发 MoviePilot `MetadataScrape` 前，会先按手动刮削路径调用 `MediaChain.recognize_by_path(..., obtain_images=True)`。
- 预识别成功时，事件会携带已预取 Fanart/logo 的 `meta` 和 `mediainfo`，避免事件内默认识别缺少 `logo_path`。
- 预识别失败时会回退旧逻辑继续触发 MoviePilot 事件刮削，不会因为 logo 预取失败而阻断补刮。

## v2.6.6 logo 附加图策略收窄
- `logo`、`long`、`landscape` 等附加图不再参与“是否刮削完整”的判断，也不进入长期复查。
- 新增媒体如果基础元数据缺失，会触发 MoviePilot 原生刮削；附加图在这次刮削中有就生成，没有就跳过。
- 长期复查继续只针对电视剧单集缺图。

## v2.6.5 新增媒体补全场景收窄
- “额外必需图片名”默认关闭，避免电影或剧名根目录因为 `long`、`logo`、`landscape` 等额外图反复补刮。
- 该配置仍保留：如果你确认某类新增媒体必须生成 `long.png`，可手动填写 `long`。
- 长期复查继续聚焦电视剧单集缺图；电影、剧信息和季信息仍只在新增入库流程中做基础检查与短期补刮。

## v2.6.4 额外图片完整度修复
- 新增“额外必需图片名”配置，可选检查 `long`。
- 电影目录或剧名根目录只有 `poster/fanart/backdrop/nfo` 时不再直接判定完整；如果配置中的额外图片缺失，会继续触发 MoviePilot 原生刮削补图。
- 如果你的 MoviePilot 实际生成的是 `logo.png` 或 `landscape.*`，可在配置里追加 `logo,landscape`；如果不需要额外图，清空该配置即可恢复旧逻辑。
